/** ESM Next config — used when package.json contains "type": "module" */
export default {
  reactStrictMode: true,
}
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true
}

module.exports = nextConfig
